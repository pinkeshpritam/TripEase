CREATE TABLE customer
(
    customer_id INT          NOT NULL,
    name        VARCHAR(255) NULL,
    age         INT          NOT NULL,
    email_id    VARCHAR(255) NULL,
    gendar      VARCHAR(255) NULL,
    CONSTRAINT pk_customer PRIMARY KEY (customer_id)
);
CREATE TABLE customer
(
    customer_id INT          NOT NULL,
    name        VARCHAR(255) NULL,
    age         INT          NOT NULL,
    email_id    VARCHAR(255) NULL,
    gendar      VARCHAR(255) NULL,
    CONSTRAINT pk_customer PRIMARY KEY (customer_id)
);